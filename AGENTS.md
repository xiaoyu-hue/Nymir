# AGENTS.md — Nymir AI 协作规则

本文件约束所有在本仓库工作的 AI Agent。作者无编程基础，依赖 Agent 协作开发。违反本规则视为不合格交付。

## 一、项目本质

Nymir 是**已上线的隐私优先 P2P 匿名聊天应用**（React 19 + TypeScript + Vite + Trystero），不是空白脚手架，禁止把任何改动当作“重写许可”。

历史基线（master，统计日 2026-09-05，仅供参考；当前数据以实际为准）：
- 提交：146
- 测试：9 个文件、73 个用例
- 源码：约 5478 行（src/，不含测试）

源码分层：
- `src/ui` — UI 层
- `src/core` — 业务逻辑
- `src/communication` — P2P 通信
- `src/persistence` — IndexedDB 存储
- `src/security` — 加密与安全

## 二、第一原则

1. **禁止 Big Bang Rewrite**。只允许：改一个模块 → 跑全量测试 → 再改下一个。
2. 优先级（从高到低）：
   用户隐私与安全 > 消息不丢失 > 测试 > 正确性 > 性能 > 代码漂亮。
3. 不确定怎么改时，先做三件事再动手：
   - 读现有实现
   - 找到对应测试
   - 确认 storage key 与事件契约

## 三、安全红线（最重要）

涉及 `src/security/` 下任一文件（`crypto.ts`、`e2ee.ts`、`e2eeManager.ts`、`sign.ts`、`noise.ts`、`pseudonym.ts`、`secureDelete.ts`、`manager.ts`）的改动：

- **必须先写测试，再改实现。** 禁止先改代码再补测试。
- 任何加密相关提交必须在提交信息中明确回答三个问题：
  1. 改的是哪一层？
  2. 攻击者能因此多知道什么？
  3. 哪个测试证明它没变坏？
- 禁止把明文（消息内容、密钥、私钥、密码）写入日志、IndexedDB、localStorage。
- 禁止用“应该安全”“理论上不可破解”描述实现。能力边界必须写清楚（例如“依赖浏览器 WebCrypto，密钥不出浏览器”）。
- README 声明的每一项安全能力，代码里必须有对应实现。文档不得超出实现。

## 四、测试纪律

- 现有测试是安全网，不得因“更干净”“重构需要”而删除。
- 禁止先改测试再改实现来逃避失败。测试失败时，必须先判断原因并写进提交信息：
  - 真 bug？
  - 测试依赖了已废弃的内部实现？
  - 有意的行为改变？
- 新增功能必须同时新增测试。
- 涉及 `src/communication` 的改动必须有行为级测试，不能只测纯函数。

## 五、克制（防过度设计）

- 只在至少 2–3 处真实重复时才抽公共模块。
- 第一版禁止引入：状态管理库、路由库、UI 组件库、ORM、DI 容器。
- 停止条件（满足任一即暂停加抽象，回来找作者确认）：
  - 测试持续回归
  - 构建产物体积明显增长
  - 模块出现大量 if/else 特判

## 六、提交规范

- git 作者必须统一为：
  `xiaoyu-hue <220487718+xiaoyu-hue@users.noreply.github.com>`
  禁止使用 `nymir@local`、`user@example.com` 等占位身份。
- 一次提交只改一层，禁止 UI + 通信 + 加密 + CSS 混在同一提交。
- 提交信息（中英文均可）必须说清：改了什么、为什么、怎么验证的。
  禁止“更新 README.md”“fix”这类零信息提交。

---

违反以上任一条款的改动，作者有权直接回滚。
